  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="brand" href="/">TaiwanSlang</a>
        <div class="nav-collapse">
          <ul class="nav">
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>

<div class="container">
<header class="jumbotron subhead" id="head">
  <h1>The Taiwanese Slang Dictionary</h1>
  <p class="lead"></p>
  <div class="row">
   <div class="span2"><a class="btn btn-primary" href="/addWord">Add new word</a></div>
   <!-- <div class="span2"><a class="btn btn-warning" href="/random">Go to random word</a></div> -->
  </div>
</header>

<section id="main">
<div class="page-header"><h1>Words</h1></div>
<% for (i=0; i < words.length; i++) { %>
 <% if (i % 12 == 0) {  %>
   <div class="row">
 <% } %>
   <div class="span1"><a href="/word/<%= words[i]._id %>"><strong><%= words[i].word %></strong></a></div>

 <% if ((i%12 == 11) || (i == (words.length-1))) {  %>
   </div>
 <% } %>

<% } %>
</section>


<div class="container">
